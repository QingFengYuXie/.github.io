{
  "name": "lightwind-navigation-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.mjs",
    "db:migrate:local": "wrangler d1 migrations apply lightwind-navigation --local",
    "db:migrate:remote": "wrangler d1 migrations apply lightwind-navigation --remote",
    "credential": "node scripts/create-credential.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
